# Deploy to these servers.
servers:
  web:
    hosts:
      - 188.245.173.162
  # job:
  #   hosts:
  #     - 192.168.0.1
  #   cmd: bin/jobs

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# Remove this section when using multiple web servers and ensure you terminate SSL at your load balancer.
#
# Note: If using Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
proxy:
  ssl: true
  host: production.localhub.solutions #change this when deploying to production (production.localhub.solutions)

# Configure the image builder.
builder:
  args:
    RAILS_ENV: production

# Use accessory services (secrets come from .kamal/secrets).
accessories:
  postgres:
    image: postgres:17
    host: 188.245.173.162
    port: 5432
    env:
      clear:
        POSTGRES_USER: "near_you"
        POSTGRES_DB: "near_you_production"
      secret:
        - POSTGRES_PASSWORD
    files:
      - config/init.production.sql:/docker-entrypoint-initdb.d/setup.sql
    directories:
      - data:/var/lib/postgresql/data
